// Reconstructed from the recorded example. Pass IDs returned by your own run through --args.
const page=figma.createPage();page.name='抖音 · Image-first 布尔复刻';await figma.setCurrentPageAsync(page);return {page:page.id};
